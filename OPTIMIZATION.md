# 项目优化方案

本方案基于 2026-07-15 对当前 `main`、构建流程以及上游
[`Samueli924/chaoxing` issues](https://github.com/Samueli924/chaoxing/issues)
的交叉审计。原则是优先修复能够由代码或脱敏样例稳定复现的问题，不把缺少平台判定依据的猜测直接带入主流程。

下文“已修复”均指本 fork 当前实现，不代表对应的上游 issue 已关闭。

## 第一阶段：正确性与防误操作（本轮已完成）

- 修复 #615 / #575：填空题改用 `answerEditor{id}{序号}` 与 `tiankongsize{id}` 提交，兼容题型代码 `2`、`10` 以及换行/`#` 分隔的多空答案。
- 修复 #612：未开放章节重试计数恢复，并重写队列 accounting，避免无限重试与 worker 异常导致的永久阻塞。
- 修复 #603：思考模型连接检查识别 `reasoning_content`，并为检查请求预留足够 token。
- 修复 #602：规范课程名中的不间断空格，并为不支持该字符的重定向输出配置安全编码。
- 缓解 #588：视频任务全局串行，避免并发心跳导致进度回溯。
- 修复 #541 的不必要版本限制：移除对 Python 3.13 `Queue.shutdown()` 的依赖，恢复 Python 3.10+ 支持。
- 无匹配课程 ID 时中止运行，避免静默处理全部课程。
- Session 在进程内稳定复用，Cookie 和连接池不再因每次 `get_session()` 被重建。
- HTTP、直播、空页面和重试耗尽等失败会向上汇总，不再无条件通知“全部完成”。

## 第二阶段：平台解析适配（后续）

- 为 #597 的 attachment/card 状态收集脱敏 HTML/JSON fixture，建立“确认完成 / 确认待办 / 未知格式”三态解析；未知格式不得当作空页面成功。
- 为 #568 的阅读任务收集 `readv2` 请求与响应 fixture，核对任务识别、时长/页数提交及成功响应语义。
- 用平台返回的 `jobCount` 与卡片元数据替代固定请求 `0..6`，降低请求量和限流概率。
- 将课程、章节、任务和题目从原始字典迁移为明确的数据模型，集中校验必需字段。
- 为验证码相关 #476 / #479 / #484 明确依赖与调用链；当前 `captcha.py` 尚未接入主流程，不能仅靠补依赖宣称已修复。

## 第三阶段：可维护性与安全（后续）

- 拆分 `api/base.py` 的登录、媒体、作业和课程服务；拆分 `api/answer.py` 的缓存、题库适配器和 AI 客户端。
- 缓存键加入题型和选项摘要，避免同标题题目复用错误答案；增加多进程文件锁。
- 统一 HTTP 超时、重试、TLS 校验和敏感字段脱敏策略。
- 引入依赖锁文件、Ruff/类型检查和 fixture 回归测试，并持续在 Python 3.10 与 3.13 上验证。

## 验证基线

本轮新增的测试覆盖：填空题表单、Session 生命周期、课程筛选、章节重试、线程异常、视频串行、直播失败、HTML 容错、LIKE 布尔配置、LLM 思考响应和通知超时。CI 会先在 Python 3.10/3.13 运行测试，再构建 Windows 可执行文件。
